pipeline {
    agent any
    tools {
        maven 'apache-maven-3.0.1' 
    }
    stages {
    stage('checkout') {
        steps {
            git 'https://github.com/ahmedelmelegy/iti-g111.git'
        }
    }

    stage('build') {
        steps {
        // One or more steps need to be included within the steps block.
        }
    }

    stage('test') {
        steps {
        // One or more steps need to be included within the steps block.
        }
    }

    stage('build image') {
        steps {
        // One or more steps need to be included within the steps block.
        }
    }

    }

}